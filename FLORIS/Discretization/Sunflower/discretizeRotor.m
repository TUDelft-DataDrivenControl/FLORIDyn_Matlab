function RPs = discretizeRotor(nRP)
%DISCRETIZEROTOR discretizes the rotor plane into nRP segments. The
% algorithm returns the normalized center location \in [-.5,.5] and the
% relative area the segment represents.
RPs = zeros(nRP,3);

end

